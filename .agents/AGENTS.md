# Project Rules

## Git Commit Directive
- DO NOT execute `git commit` or `git push` automatically after modifying code.
- Only perform `git commit` or `git push` when the user explicitly requests it.
